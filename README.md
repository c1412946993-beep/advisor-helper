# Financial Advisor Interview Generator

一个帮助用户选择合适的财务顾问的免费工具。通过 1 分钟的小测验，帮助用户确定是需要机器人顾问还是 CFP（注册财务规划师），并生成一系列面试问题。

## 功能特性

- 🎯 1 分钟快速测验
- 🤖 智能推荐：机器人顾问 vs CFP
- 📝 生成个性化面试问题
- 📊 数据分析和追踪
- 💰 避免高额费用和诈骗

## 技术栈

- React 18
- TypeScript
- Vite
- Tailwind CSS
- Supabase
- Lucide React Icons

## 本地开发

### 前置要求

- Node.js 18+ 
- npm 或 yarn

### 安装依赖

```bash
npm install
```

### 环境变量

创建 `.env` 文件并配置以下环境变量：

```env
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
```

### 运行开发服务器

```bash
npm run dev
```

### 构建生产版本

```bash
npm run build
```

### 预览生产构建

```bash
npm run preview
```

## 部署到 Vercel

1. 将项目推送到 GitHub
2. 在 Vercel 中导入项目
3. 配置环境变量：
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY`
4. 部署即可

Vercel 会自动检测 Vite 项目并使用正确的构建配置。

## 项目结构

```
├── src/
│   ├── components/     # 可复用组件
│   ├── pages/          # 页面组件
│   ├── data/           # 数据文件
│   ├── lib/            # 工具库（Supabase 客户端等）
│   └── App.tsx         # 主应用组件
├── public/             # 静态资源
├── supabase/           # Supabase 迁移文件
└── dist/               # 构建输出（生产环境）
```

## 许可证

MIT

